import 'package:flutter/material.dart';

// Background Colors
const Color lightBackground = Color(
  0xFFFDFCF5,
); // Light cream for the entire app
const Color darkBackground = Color(
  0xFF2A2A2A,
); // Dark gray for dark mode contrast

// Text Colors
const Color lightTextColor = Color(
  0xFF2C2C2C,
); // Dark gray text for better readability
const Color darkTextColor = Color(0xFFFFFFFF); // White for dark mode

// Secondary Text Colors
const Color lightSecondaryTextColor = Color(
  0xFF9E9E9E,
); // Light gray for secondary text
const Color darkSecondaryTextColor = Color(
  0xFFB0B0B0,
); // Soft gray for dark mode

// Button Colors
const Color lightButtonColor = Color(0xFF32A852); // Primary green button color
const Color darkButtonColor = Color(
  0xFF388E3C,
); // Darker green for buttons in dark mode

// Secondary Button Colors
const Color lightSecondaryButtonColor = Color(
  0xFFBDBDBD,
); // Muted gray for secondary buttons
const Color darkSecondaryButtonColor = Color(
  0xFF616161,
); // Darker gray for secondary buttons

// Accent Colors
const Color lightAccentColor = Color(0xFFF4F1EB); // Beige for subtle accents
const Color darkAccentColor = Color(
  0xFF8D8D8D,
); // Muted gray for dark mode accents

// Error Colors
const Color lightErrorColor = Color(0xFFEF5350); // Bright red for errors
const Color darkErrorColor = Color(
  0xFFB71C1C,
); // Darker red for errors in dark mode

// App Bar Colors
const Color lightAppBarColor = Color(
  0xFFFDFCF5,
); // Match background for seamless app bar
const Color darkAppBarColor = Color(0xFF424242); // Dark gray for dark mode

// Card Background Colors
const Color lightCardColor = Color(
  0xFFFFFFFF,
); // Pure white for clean card appearance
const Color darkCardColor = Color(
  0xFF616161,
); // Dark gray for cards in dark mode

// Input Field Colors
const Color lightInputFieldColor = Color(0xFFF4F1EB); // Beige for input fields
const Color darkInputFieldColor = Color(
  0xFF757575,
); // Dark gray for input fields in dark mode

// Green Colors
const Color lightGreenColor = Color(
  0xFF32A852,
); // Vibrant green for highlights and buttons
const Color darkGreenColor = Color(
  0xFF2E7D32,
); // Darker green for consistency in dark mode

// Button Colors
const Color primaryButtonColor = Color(0xFF00796B); // Teal for primary buttons
const Color secondaryButtonColor = Color(0xFFF4A261); // Muted mustard yellow
const Color accentButtonColor = Color(0xFFFF6F00); // Vibrant orange for accents

// Bottom Navigation Bar Colors
const Color bottomNavigationbarColor = Color(
  0xFFFDFCF5,
); // Light cream for navigation bar
