import 'package:flutter/material.dart';

// Background Colors
const Color lightBackground = Color(0xFF092727);
const Color darkBackground = Color(0xFF051616);

// Text Colors
const Color headingText = Color(0xFFFFFFFF); // White for contrast
const Color normalText = Color(0xFFE0E0E0); // Light gray for readability
const Color subtleText = Color(0xFFB0B0B0); // Subtle gray for secondary text

// Button Colors
const Color primaryButton = Color(0xFF14B8B8); // Bright teal
const Color secondaryButton = Color(0xFF1A4747); // Lighter teal
const Color buttonText = Color(0xFFFFFFFF); // White text on buttons

// Status Colors
const Color success = Color(0xFF00C853); // Green
const Color error = Color(0xFFFF5252); // Red
const Color warning = Color(0xFFFFB74D); // Orange

// Border Colors
const Color border = Color(0xFF1A4747); // Subtle teal border
const Color divider = Color(0xFF133535); // Darker border for dividers

// Navigation Colors
const Color appBar = Color(0xFF0B2F2F); // Slightly lighter than background
const Color bottomNavBar = Color(0xFF0B2F2F); // Matching app bar
const Color activeNavItem = Color(0xFF14B8B8); // Bright teal for active items
const Color inactiveNavItem =
    Color(0xFF88A3A3); // Muted teal for inactive items

// Overlay Colors
const Color modalBackground = Color(0xFF072222); // Darker than main background
const Color tooltip = Color(0xFF1A4747); // Lighter teal for tooltips

// Input Field Colors
const Color inputBackground = Color(0xFF0F3333);
const Color inputBorder = Color(0xFF1A4747);
const Color placeholder = Color(0xFF88A3A3);
